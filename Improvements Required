CheckList of improvmentes taht needs to be done and tested 

====================================================================================
====================================================================================
FROM: 03/03/2026
https://github.com/X1ndy02/RaspPi5_Home_Lab/blob/main/tests/ups_natural_shutdown.md
------------------------------------------------------------------------------------

1. Fix shutdown backup script
The quick backup failed with exit code 126.
The system is not protected during power loss until this works.

2. Identify and fix force-killed container
One container required forced termination (exit 137).
Find which container it was and ensure it shuts down gracefully.
(The forced‑terminated container was nextcloud-clamav-1
  (container ID prefix f388092f38e1).)

3. Fix AC_LOST logging
The power loss event was not recorded in the UPS log.
Event logging must reflect real power events.

4. Improve shutdown email logging
Shutdown email was received but no corresponding msmtp log entry exists.
Make email logging consistent and verifiable.

5. Manually validate backup script
Run ups-quick-backup.sh manually outside of shutdown.
Confirm it executes correctly before retesting UPS behaviour.

6. Retest full shutdown after fixes
Repeat battery drain test after correcting backup script.
Verify:
- Backup completes successfully
- No containers are force-killed
- AC_LOST event is logged

7. Consider increasing Docker stop timeout
If containers require more than 10 seconds to exit cleanly,
adjust stop timeout configuration.

8. Add healthchecks to critical containers
Add Docker healthchecks for:
- MariaDB
- Nextcloud app
- Reverse proxy
So failures are detected earlier.
====================================================================================
====================================================================================

